+b.comments(id="comments")
  +e.header
    +e("h2").header-title
      a(href="#comments" name="comments")= t('Comments')
    +e.read-before
      +e("span").read-before-link= t('read me before writing…')
      +e.read-before-popup
        +e.read-before-popup-i
          ul
            li= t("You're welcome to post additions, questions to the articles and answers to them.")
            li!= t("To insert a few words of code, use the <code>&lt;code&gt;</code> tag, for several lines – use <code>&lt;pre&gt;</code>, for more than 10 lines – use a sandbox (<a href='http://plnkr.co/edit/?p=preview'>plnkr</a>, <a href='http://jsbin.com'>JSBin</a>, <a href='http://codepen.io'>codepen</a>…)")
            li= t("If you can't understand something in the article – please elaborate.")


#disqus_thread

//- disqus_url gets correct host only in production
if env.NODE_ENV == 'production'
  script var disqus_shortname = !{escapeJSON(disqusDomain)};
  script var disqus_identifier = !{escapeJSON(canonicalPath)};
  script var disqus_title = !{escapeJSON(headTitle || title)};
  script var disqus_url = !{escapeJSON(canonicalUrl)};

  script(async src='//' + disqusDomain + '.disqus.com/embed.js')
  script(async src='//' + disqusDomain + '.disqus.com/count.js')
