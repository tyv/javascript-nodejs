include /bem

+b.login-form(data-form="login")
  +e('form').form(action="#")
    +e.line.__header
      +e('h4').title Вход в систему
      +e.header-aside
        a(href="#" data-switch="register-form") регистрация
    +e.line.__notification(data-notification)
    +e.line
      +e('label').label(for="login") Имя пользователя или Email:
      +b('span').text-input.__input
        +e('input').control#login(name="login")
    +e.line
      +e('label').label(for="password") Пароль:
      +b('span').text-input.__input
        +e('input').control#password(type="password", name="password")
    +e.line.__footer
      +e.button
        +b('button').submit-button.__submit(type="submit")
          +e('span').text Войти
      +e.footer-link
        a(href="#" data-switch="forgot-form") Забыли пароль?
    +e.line.__social-login
      +e('h5').social-login-title Вход через социальные сети
      =" "
      +b('a').social-login._yandex(href="#" data-provider="yandex") Yandex
      =" "
      +b('a').social-login._vkontakte(href="#" data-provider="vkontakte") Вконтакте
      =" "
      +b('a').social-login._facebook(href="#" data-provider="facebook") Facebook
      =" "
      +b('a').social-login._google(href="#" data-provider="google") Google+
      =" "
      +b('a').social-login._github(href="#" data-provider="github") Github
    +b('button').close-button.__close(type="button", title="закрыть") закрыть

