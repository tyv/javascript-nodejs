#!/usr/bin/env node

const log = require('js-log')();
const config = require('config');
const co = require('co');
const app = require('app');

co(function*() {

  yield* app.run();

})(function(err) {
  if (err) throw err;
});
